/* jquery.min.js - placeholder
 * Replace with the real jQuery build or load it from a CDN.
 */

// Minimal helper placeholder
window.$ = window.jQuery = function(selector){ return document.querySelector(selector); };
